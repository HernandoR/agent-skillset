{
  "hooks": [
    {
      "event": "SessionEnd",
      "command": "${CLAUDE_PLUGIN_ROOT}/hooks/commit-reminder.sh",
      "description": "Warn about uncommitted tracked changes when a session ends"
    }
  ]
}