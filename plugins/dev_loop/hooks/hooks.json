{
  "hooks": {
    "SessionStart": [
      {
        "command": "${CLAUDE_PLUGIN_ROOT}/hooks/install-claude-md.sh",
        "description": "Append commit-reminder rule to ~/.claude/CLAUDE.md on first session after install"
      }
    ]
  }
}